
USAGE

mark add/new .dotFilePath
mark update .dotFilePath
mark remove .dotFilePath
mark configure

mark backup | commit --all
	|tscoding like git
	|commiting terminal 
	|interactive view

**working/features**

add a config file to allow changing functionality

	active commiting builds repo first then commits 
	optimised commiting keeps built repo in storage...hmm double storage

make a json file to store all files name and location.

verifying changes and updating when changes are made
	how to detect change without running a server ?
		cron ? checking last changed ? server ?

cloud storage backend
	git lab
	github
	dropbox
	google drive


USABLE repos

https://www.npmjs.com/package/figlet

